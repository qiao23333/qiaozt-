# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

"乌鹊南飞"（个人开发者）负责写作、发布项目记录与维护本站；访客在桌面端或移动端阅读文章、浏览作品与友链更新、查看项目和工具，并通过站内留言或邮箱与我交流。

## Product Purpose

"乌鹊南飞"是一个以 Astro 静态构建的个人世界，用来沉淀技术实践、作品项目、生活随笔和长期写作。它也把独立博客友链与 RSS 更新聚合到"朋友圈"，让访客更容易发现其他作者。成功意味着我可以低摩擦地发布和维护内容，访客可以清晰阅读、顺畅浏览并参与交流。

## Positioning

这是一个把个人写作、项目展示、作品集、友链关系、朋友圈聚合和互动放在同一处的个人站点。它保留强烈的作者身份和内容语气，同时以仓库配置与静态构建为主要维护方式，而不是依赖重型 CMS 后台。

## Operating Context

- 源码在本地 Astro 开发环境中预览，文章和配置在 Git 仓库中维护；可选的自动化任务用于刷新朋友圈快照或执行构建流程。
- 文章以 Markdown/Astro 内容文件维护，站点与页面开关、作品、友链、评论等配置位于 `src/config/`。
- "朋友圈"从已配置友链的 RSS/Atom 读取最近文章，并使用 `src/data/friends-feed-snapshot.json` 保存可复用的 feed 快照；站点暂时不可达或没有订阅地址时，应保留可解释的状态而不阻塞页面。
- 评论区默认关闭；统计、评论等外部服务在未配置时不加载，避免拖慢页面。
- 部署目标是主流静态托管（Vercel / Cloudflare Pages 等），不依赖任何原作者的个人服务器。

## Capabilities and Constraints

- Astro 静态站点，包含文章、归档、分类、标签、作品、友链、朋友圈、留言板、关于、更新日志、相册、打赏等页面。
- 作品页是配置驱动的：项目数据来源是 `src/config/projectsConfig.ts`（并支持从我的 GitHub 公开仓库自动拉取），新增作品只需新增仓库或配置记录，无需改动页面样式。
- 桌面端和移动端都必须可用；作品跳转、文章阅读、导航、友链卡片和朋友圈分页是核心浏览路径。
- 站点使用 Swup 的客户端页面切换；只在特定页面生效的粒子、动画和事件监听必须在路由切换后重新初始化，并在离开页面时清理资源。
- 没有内容、没有配置的页面应显示清晰的空状态占位，而不是变成不可访问的 404 或空白页。
- 友链支持本站、推荐友链和普通友链等身份差异化表达；外部站点的名称、描述、头像、RSS 和截图应以真实配置为准，不应凭空补全。
- 评论系统如需启用，应使用自己的后端；留言交互不依赖原作者的评论或统计服务。

## Brand Commitments

- 站点名称：乌鹊南飞；作者身份使用"乌鹊南飞"。
- 站点副题记为"月明星稀，乌鹊南飞"（出自《短歌行》），保持中文优先、真诚而带有个人叙事的表达。
- 站点头像、logo、favicon 使用我的 GitHub 头像；不保留、不使用原作者的个人视觉素材与身份资产。
- 用户个人头像、友链、相册、打赏收款码等属于个人身份资产，只展示真实素材；未提供时显示占位而非沿用他人内容。

## Evidence on Hand

- 真实文章内容位于 `src/content/`。
- 页面与站点配置位于 `src/pages/`、`src/config/` 和 `src/components/`；当前路由包含 `/about/`、`/works/`、`/friends/`、`/moments/`、`/guestbook/`、`/hire/`、`/gallery/` 和 `/changelog/` 等。
- 作品配置位于 `src/config/projectsConfig.ts`，GitHub 自动拉取逻辑在 `src/pages/works.astro`。
- 友链配置位于 `src/config/friendsConfig.ts`，朋友圈快照位于 `src/data/friends-feed-snapshot.json`。
- `README.md` 是本站的维护说明；它对站点身份、作品层和布局层有清晰划分。

## Product Principles

- 先保护作者真实内容与身份，再做视觉增强。
- 站点必须是"我自己的"，不应原样保留两端原作者的个人内容、域名预览或站点标识。
- 让本地编辑、预览、提交和构建保持低摩擦、可回退。
- 把阅读清晰度、移动端可用性和导航稳定性放在装饰效果之前。
- 外部 RSS、图片和评论服务失败时要有明确、不中断主流程的降级状态。
- 页面切换、粒子和动画必须可重复初始化并释放资源，避免缓存或 Swup 导致"本地正常、线上偶发"的状态。

## Accessibility & Inclusion

- 桌面和移动布局都应保持可读、可滚动和可操作，交互控件需要键盘可达并保留清晰焦点状态。
- 动画只用于表达状态和氛围，不应成为理解内容或完成导航的唯一条件；在动态效果不可用时，文字、分页和身份信息仍需完整显示。
- 评论输入、错误提示、RSS 失联提示和空状态要使用清楚的中文说明，避免把外部服务异常伪装成用户操作失败。