# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

“朝朝听雨”的站长（Rain／独立开发者）负责写作、发布项目记录和维护博客；访客在桌面端或手机端阅读文章、浏览友链与朋友圈更新、查看工具和项目，并通过 Waline 留言交流。

## Product Purpose

“朝朝听雨”是一个以 Astro 静态构建的个人博客，用来沉淀技术实践、自动化项目、生活随笔和长期写作。它也把独立博客友链与 RSS 更新聚合到“朋友圈”，让访客更容易发现其他作者。成功意味着站长可以低摩擦地发布和维护内容，访客可以清晰阅读、顺畅浏览并参与交流。

## Positioning

这是一个把个人写作、项目展示、友链关系、朋友圈聚合和评论互动放在同一处的个人站点。它保留强烈的作者身份和内容语气，同时以仓库配置与静态构建为主要维护方式，而不是依赖重型 CMS 后台。

## Operating Context

- 源码在本地 Astro 开发环境中预览，文章和配置在 Git 仓库中维护；GitHub Actions 可按计划刷新朋友圈快照并执行构建流程。
- 文章以 Markdown/Astro 内容文件维护，站点与页面开关、友链、评论等配置位于 `src/config/`。
- “朋友圈”从已配置友链的 RSS/Atom 读取最近文章，并使用 `src/data/friends-feed-snapshot.json` 保存可复用的 feed 快照；站点暂时不可达或没有订阅地址时，应保留可解释的状态而不阻塞页面。
- Waline 提供文章评论和留言板，Umami 提供访客与浏览统计；AI 辅助功能用于理解内容或提供站内回复，但不替代站长观点。

## Capabilities and Constraints

- Astro 静态站点，包含文章、归档、分类、标签、友链、朋友圈、留言板、关于、工具、更新日志、相册等页面。
- 桌面端和移动端都必须可用；文章阅读、导航、评论表单、友链卡片和朋友圈分页是核心浏览路径。
- 站点使用 Swup 的客户端页面切换；只在特定页面生效的粒子、动画和事件监听必须在路由切换后重新初始化，并在离开页面时清理资源。
- 朋友圈默认每页显示 20 篇更新，内容来自构建时快照而非每次访问都实时请求所有友链。
- 友链支持本站、推荐友链和普通友链等身份差异化表达；外部站点的名称、描述、头像、RSS 和截图应以真实配置为准，不应凭空补全。
- 评论系统要求填写昵称和邮箱；匿名评论关闭。访客数据和邮件、AI 等密钥属于服务端配置，不应进入前端源码、公开静态目录或 Git 历史。

## Brand Commitments

- 站点名称：朝朝听雨；作者身份同时使用 Rain／朝朝听雨。
- 站点副题记为“物物而不物于物，念念而不念于念”，保持中文优先、真诚而带有个人叙事的表达。
- 现有的鸣潮／二次元视觉素材、作者头像、文章封面和站点动画是已提供的身份资产；后续改动应优先复用真实素材，不用泛化占位图替代。
- 友链与评论的特殊身份（本站主理人、AI 回复、推荐友链）可以有独立的视觉提示，但不能牺牲内容可读性或制造无法核验的事实。

## Evidence on Hand

- 真实文章内容位于 `src/content/posts/`，包含技术项目、博客改造、自动化实践和个人写作。
- 页面与站点配置位于 `src/pages/`、`src/config/` 和 `src/components/`；当前路由包含 `/about/`、`/friends/`、`/moments/`、`/guestbook/`、`/tools/` 和 `/changelog/` 等。
- 友链配置位于 `src/config/friendsConfig.ts`，朋友圈快照位于 `src/data/friends-feed-snapshot.json`。
- `README.md` 仍是 Firefly 主题基线文档；它不应覆盖这个二创站点已经形成的内容、身份和交互事实。
- 没有把外部评论、访问统计或 RSS 可用性当作固定数据；这些数据需要在对应服务或下一次构建时重新验证。

## Product Principles

- 先保护作者真实内容与身份，再做视觉增强。
- 让本地编辑、预览、提交和构建保持低摩擦、可回退。
- 把阅读清晰度、移动端可用性和导航稳定性放在装饰效果之前。
- 外部 RSS、图片和评论服务失败时要有明确、不中断主流程的降级状态。
- 页面切换、粒子和动画必须可重复初始化并释放资源，避免缓存或 Swup 导致“本地正常、线上偶发”的状态。

## Accessibility & Inclusion

- 桌面和移动布局都应保持可读、可滚动和可操作，交互控件需要键盘可达并保留清晰焦点状态。
- 动画只用于表达状态和氛围，不应成为理解内容或完成导航的唯一条件；在动态效果不可用时，文字、分页和身份信息仍需完整显示。
- 评论输入、错误提示、RSS 失联提示和空状态要使用清楚的中文说明，避免把外部服务异常伪装成用户操作失败。
