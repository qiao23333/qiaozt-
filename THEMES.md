# 主题管理机制（方案一：构建时切换）

本站采用 **git 快照 + 分支** 管理多主题：内容与配置随分支走，随时保存、随时切回。

## 当前主题

| 主题 | 名称 | 快照 | 状态 |
|------|------|------|------|
| 主题 1 | Firefly 二改（萤火虫主题深度定制版） | 标签 `theme-1-firefly-baseline` | **使用中**（在 main 分支上继续修改） |

主题 1 的定稿状态已永久保存在 GitHub 标签 `theme-1-firefly-baseline` 中，
无论之后怎么改，随时可以回到这个样子。

## 工作流程

### 修改主题 1（现在）
直接在 main 分支上改即可，正常提交推送。每个阶段性定稿都可以再打一个标签，例如：

```powershell
git tag -a theme-1-v2 -m "主题1第二版：xxx改动"
git push origin theme-1-v2
```

### 创建主题 2（当你对我说"开始创建新主题"时）

我会自动执行：

1. **定稿主题 1**：把当时 main 上所有未提交的改动提交推送，打标签存档
2. **冻结主题 1**：创建分支 `theme/1-firefly` 并推送——主题 1 的活体备份
3. **开做主题 2**：在 main 上开发新主题（新布局/组件/风格），内容与配置照常共享
4. **部署切换**：主题 2 完成后构建部署即是新站；想回主题 1 见下

### 随时切回主题 1

```powershell
# 方式 A：整个站切回主题 1（部署哪个分支，线上就是哪个主题）
git checkout theme/1-firefly
# 构建部署后线上即恢复主题 1

# 方式 B：只把主题 1 的"外观"带回当前分支，保留主题 2 期间写的新文章
#   主题 2 期间写的新文章、新配置先留在 main，然后：
git checkout theme/1-firefly -- src/layouts src/components src/styles
# 提交后 main 就是"主题 1 外观 + 全部最新内容"
```

**注意**：文章在 `src/content/`、站点设置在 `src/config/`，这两处与主题无关、全主题共享。
切主题不会丢文章；若主题 2 期间写了新文章想带回主题 1，用方式 B 或单独合并 content 目录。

### 与主题无关、换主题时**不会**丢的东西

- `src/content/` 全部文章与关于页文案
- `src/config/` 站点设置（导航/公告/友链/相册/音乐…）
- `public/` 头像、favicon、相册图片
- `CUSTOMIZE.md` 自定义指南

## 与「访客端换肤」的区别

本机制是**站长级**主题切换：换主题 = 重新构建 + 部署，全站访客看到新主题。
访客自己能调的（亮暗模式/主题色相/壁纸）由 `siteConfig.ts` 提供，与多主题机制无关。

## 已知约束

- 切换主题需要重新构建（本地内存紧张时可在 Vercel/Cloudflare Pages 云端构建，见 `DEPLOY.md`）
- 每套主题是完整的一套外观，体量约为主仓库的 src 部分；建议同一时期主推一套主题，避免双线维护
