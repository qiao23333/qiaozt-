# 素材插槽清单（乌鹊南飞 · 个人站）

> 原则：**不擅自生成图片，不沿用原作者素材**。下表列出的每一个插槽都等待你提供正式素材；在提供之前站点使用占位/降级状态，不引入作者图片。
> 放置规则：`.webp/.png/.jpg/.svg` 等静态素材放在 `public/` 下（原样输出）；需要 Astro 优化的放 `src/assets/`（构建自动压缩）。

## 一、待替换的作者残留素材（当前站点仍在使用，需优先替换）

| # | 用途 | 当前引用路径 | 改为 |
|---|------|------|------|
| A0 | 个人头像 | `src/assets/images/rain-avatar.webp`（配置在 `profileConfig.ts`） | 你的头像 |
| A1 | 导航栏 Logo | `src/assets/images/chaoc-tingyu-avatar.webp`（配置在 `siteConfig.ts` navbar.logo） | 你的 Logo |
| A2 | 站点图标 favicon | `public/favicon/chaoc-tingyu-avatar-{512,192,180,32}.png` | 你的站点图标 |

> 替换方法：把新文件放进对应目录，改 `profileConfig.ts` 的 `avatar`、`siteConfig.ts` 的 `navbar.logo.value` 与 `favicon` 数组路径即可。

## 二、需要你提供的正式素材插槽

| 插槽 | 用途 | 推荐尺寸 | 格式 | 是否需要手机端专用 | 缺省降级 |
|------|------|------|------|------|------|
| 个人头像 | 主页/侧边栏个人卡片 | 512×512（方形） | webp/png | 否 | 占位色块 |
| 站点 Logo | 导航栏标识 | 64×64 / 128×128 透明 | svg/png | 否 | 文字标题 |
| 站点图标 favicon | 浏览器标签/收藏 | 32 / 180 / 192 / 512 | png | 否 | 占位图标 |
| 首页横幅 / Hero | 首页顶部大图 | 1920×(520~1080) 或 16:9 | webp | 否 | 纯色/壁纸 |
| 桌面全屏壁纸 | 站点背景（可选） | ≥1920×1080（16:9） | webp | 否 | 纯色 |
| 手机壁纸 | 移动端背景（可选） | ≥1080×2340 | webp | 是（单独一张） | 纯色 |
| 文章默认封面 | 无封面文章的兜底 | 1200×630（约 1.91:1） | webp | 否 | 渐变占位 |
| 单篇文章封面 | 每篇可选配 | 1200×630 或 16:9 | webp | 否 | 用默认封面 |
| 项目封面 | 作品区/案例卡片 | 16:9（如 1280×720） | webp | 否 | 用默认封面 |
| 音乐封面 | 播放器/歌单 | 512×512（方形） | webp | 否 | 占位色块 |
| 社交分享图 | 外链分享缩略 (OG) | 1200×630 | png/webp | 否 | 站点主题卡 |
| 页脚装饰 | 底部小图（可选） | 小尺寸偶数宽 | svg/webp | 否 | 无 |

## 三、待办：作者账号数据要改或关闭

| 位置 | 当前仍是作者的 | 处理 |
|------|------|------|
| `siteConfig.ts` → `bangumi.userId` | `1143164` | 换成你的 Bangumi 账号，或关闭番组页面（`siteConfig.pages.bangumi=false`） |
| `siteConfig.ts` → `anime.bilibili.uid` | `38932988` | 换成你的 B 站 UID，或关闭追番页面（`siteConfig.pages.anime=false`） |

## 四、待办：域名

- `siteConfig.ts` → `site_url` 目前是占位 `https://your-site.com`。拿到正式域名后改为完整地址（影响 RSS/canonical/分享链接），未部署时不臆造域名。

## 五、可选的作者项（非必需，按需开通）

- 评论区（需求文档列为非 V1 必需）等，需要时再接入第三方服务。